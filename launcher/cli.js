#!/usr/bin/env node
/**
 * PlanningTree CLI launcher (stub — Phase 6 will add binary download + port selection)
 *
 * In production: downloads platform-specific PyInstaller binary on first run,
 * spawns it on a free port, and opens the browser.
 *
 * For local development: use `python scripts/dev.py` instead.
 */

console.log('PlanningTree launcher')
console.log('This is the development stub.')
console.log('')
console.log('To start the app during development:')
console.log('  python scripts/dev.py')
console.log('')
console.log('Production distribution will be implemented in Phase 6.')
process.exit(0)
